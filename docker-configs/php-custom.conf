[PHP]
; File upload settings for task attachments
; Support up to 20MB for video files, 10MB for other files
upload_max_filesize = 20M
post_max_size = 25M
max_file_uploads = 20
max_execution_time = 300
memory_limit = 256M

; Other useful settings
max_input_time = 60
max_input_vars = 1000
